<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Rapport Technicien</title>
    <style>
        body { font-family: 'Helvetica', sans-serif; color: #1e293b; margin: 0; padding: 20px; line-height: 1.5; }
        .header { text-align: center; margin-bottom: 40px; border-bottom: 3px solid #f59e0b; padding-bottom: 20px; }
        .header h1 { color: #f59e0b; margin: 0; font-size: 28px; text-transform: uppercase; }
        .header p { color: #64748b; margin: 5px 0 0; font-size: 14px; }
        
        .info-grid { width: 100%; margin-bottom: 30px; }
        .info-box { border: 1px solid #e2e8f0; padding: 15px; border-radius: 8px; background-color: #f8fafc; }
        .info-box h3 { margin: 0 0 10px; color: #0f172a; border-bottom: 1px solid #cbd5e1; padding-bottom: 5px; }
        .info-box p { margin: 5px 0; font-size: 13px; }
        
        .stats-container { margin-bottom: 30px; clear: both; width: 100%; height: 100px; }
        .stat-card { float: left; width: 30%; border: 1px solid #e2e8f0; padding: 15px; border-radius: 8px; text-align: center; margin-right: 3%; background-color: #fff; }
        .stat-card:last-child { margin-right: 0; }
        .stat-card h4 { margin: 0 0 10px; color: #64748b; font-size: 12px; text-transform: uppercase; }
        .stat-value { font-size: 24px; font-weight: bold; color: #1e293b; }
        .stat-value.success { color: #10b981; }
        
        h3.section-title { margin: 30px 0 15px; color: #0f172a; font-size: 18px; }
        
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th, td { border: 1px solid #e2e8f0; padding: 10px; text-align: left; font-size: 11px; }
        th { background-color: #f1f5f9; color: #475569; font-weight: bold; text-transform: uppercase; font-size: 10px; }
        tr:nth-child(even) { background-color: #f8fafc; }
        
        .priority-badge { display: inline-block; padding: 2px 6px; border-radius: 4px; font-size: 9px; font-weight: bold; }
        .priority-urgente { background-color: #fee2e2; color: #991b1b; }
        .priority-elevee { background-color: #fef3c7; color: #92400e; }
        .priority-normale { background-color: #dcfce7; color: #166534; }
        
        .footer { margin-top: 60px; text-align: center; font-size: 10px; color: #94a3b8; border-top: 1px solid #e2e8f0; padding-top: 10px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Rapport de Performance</h1>
        <p>Système de Gestion de Maintenance Assistée par Ordinateur (GMAO)</p>
    </div>

    <div class="info-box">
        <h3>Informations Technicien</h3>
        <p><strong>Nom complet:</strong> {{ technicien.user.nom }} {{ technicien.user.prenom }}</p>
        <p><strong>Spécialité:</strong> {{ technicien.specialite }}</p>
        <p><strong>Email:</strong> {{ technicien.user.email }}</p>
        <p><strong>Date de génération:</strong> {{ "now"|date("d/m/Y H:i") }}</p>
    </div>

    <h3 class="section-title">Indicateurs Clés</h3>
    <div class="stats-container">
        <div class="stat-card">
            <h4>Interventions</h4>
            <div class="stat-value">{{ interventions|length }}</div>
        </div>
        <div class="stat-card">
            <h4>Terminées</h4>
            <div class="stat-value success">{{ interventions|filter(i => i.statut == 'Terminee')|length }}</div>
        </div>
        <div class="stat-card">
            <h4>CA Généré</h4>
            <div class="stat-value">{{ totalRevenue|number_format(2, ',', ' ') }} €</div>
        </div>
    </div>

    <h3 class="section-title">Historique des Interventions</h3>
    <table>
        <thead>
            <tr>
                <th>Réf</th>
                <th>Date</th>
                <th>Machine</th>
                <th>Priorité</th>
                <th>Statut</th>
                <th>Coût MO</th>
            </tr>
        </thead>
        <tbody>
            {% for intervention in interventions %}
                <tr>
                    <td>#{{ intervention.id }}</td>
                    <td>{{ intervention.dateDebut|date("d/m/Y") }}</td>
                    <td>{{ intervention.machine.modele }}</td>
                    <td>
                        <span class="priority-badge {% if intervention.priorite == 'Urgente' %}priority-urgente{% elseif intervention.priorite == 'Elevee' %}priority-elevee{% else %}priority-normale{% endif %}">
                            {{ intervention.priorite }}
                        </span>
                    </td>
                    <td>{{ intervention.statut }}</td>
                    <td>{{ (intervention.coutMainOeuvre ?? 0)|number_format(2, ',', ' ') }} €</td>
                </tr>
            {% endfor %}
            {% if interventions|length == 0 %}
                <tr>
                    <td colspan="6" style="text-align: center; color: #94a3b8;">Aucune intervention enregistrée</td>
                </tr>
            {% endif %}
        </tbody>
    </table>

    <div class="footer">
        © {{ "now"|date("Y") }} MaintenancePro - Logiciel de GMAO Professionnel. Ce document est strictement confidentiel.
    </div>
</body>
</html>
