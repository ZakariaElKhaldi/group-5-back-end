<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Historique Machine {{ machine.reference }}</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 14px; color: #333; }
        .header { text-align: center; margin-bottom: 30px; border-bottom: 2px solid #ddd; padding-bottom: 15px; }
        h1 { color: #1e293b; font-size: 24px; margin: 0; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th, td { padding: 10px; text-align: left; border-bottom: 1px solid #e2e8f0; }
        th { background-color: #f1f5f9; font-weight: bold; color: #475569; }
        tr:nth-child(even) { background-color: #f8fafc; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Historique Machine: {{ machine.reference }}</h1>
        <p>Modèle: {{ machine.modele }} | Marque: {{ machine.marque }}</p>
    </div>

    <h2>Interventions</h2>
    {% if machine.interventions is empty %}
        <p>Aucune intervention enregistrée.</p>
    {% else %}
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Date</th>
                    <th>Type</th>
                    <th>Technicien</th>
                    <th>Statut</th>
                </tr>
            </thead>
            <tbody>
                {% for intervention in machine.interventions %}
                    <tr>
                        <td>#{{ intervention.id }}</td>
                        <td>{{ intervention.dateDebut ? intervention.dateDebut|date('d/m/Y') : '-' }}</td>
                        <td>{{ intervention.type }}</td>
                        <td>{{ intervention.technicien.user.prenom }} {{ intervention.technicien.user.nom }}</td>
                        <td>{{ intervention.statut }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
</body>
</html>
